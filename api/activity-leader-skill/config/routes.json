{
  "routes": [
    {
      "method": "GET",
      "path": "/activity-leader-skills",
      "handler": "activity-leader-skill.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/activity-leader-skills/count",
      "handler": "activity-leader-skill.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/activity-leader-skills/:id",
      "handler": "activity-leader-skill.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/activity-leader-skills",
      "handler": "activity-leader-skill.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/activity-leader-skills/:id",
      "handler": "activity-leader-skill.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/activity-leader-skills/:id",
      "handler": "activity-leader-skill.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
